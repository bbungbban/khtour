<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ksmart42.khtour.mapper.RecordBoardMapper">

	<resultMap type="RecordBoard" id="recordBoardResultMap">
		<id column="record_board_code" property="recordBoardCode"/>							<!-- 여행게시판 코드 -->
		<result column="member_id" property="memberId"/>									<!-- 멤버 아이디 -->										
		<result column="record_board_name" property="recordBoardName"/>						<!-- 여행게시판 시작일 -->
		<result column="content" property="content"/>										<!-- 여행게시판 시작일 -->
		<result column="status" property="status"/>											<!-- 여행 종료일 -->
		<result column="image" property="image"/>											<!-- 개인/단체 구분 -->
		<result column="start_date" property="startDate"/>									<!-- 여행 준비물 -->
		<result column="end_date" property="endDate"/>										<!-- 여행 총 경비 -->
		<result column="reg_date" property="regDate"/>										<!-- 여행 지역 -->
		<result column="like_count" property="likeCount"/>									<!-- 여행 지역 -->
		<result column="views" property="views"/>											<!-- 여행 지역 -->					
	</resultMap> 
	<!-- 문화재 정보 삭제 SQL -->
	<delete id="removeRecordBoard" parameterType="String">
		DELETE
		FROM
			tb_record_board
		WHERE
			record_board_code = #{recordBoardCode};
	</delete>


	<!-- 문화재 목록 조회 SQL -->
	<select id="getRecordBoardList"  resultMap="recordBoardResultMap">
		SELECT
		  r.record_board_code
		   ,r.member_id
		   ,r.record_board_name
		   ,r.content
		   ,r.status    
		   ,r.image
		   ,r.start_date   
		   ,r.end_date
		   ,r.reg_date    
		   ,r.like_count    
		   ,r.views    
		FROM
		  tb_record_board AS r;
	</select>
	
	<!-- 코드에 따른 문화재 조회 SQL -->
	<select id="getRecordBoardByCode" parameterType="String" resultMap="recordBoardResultMap">
		SELECT
		    r.record_board_code
		   ,r.member_id
		   ,r.record_board_name
		   ,r.content
		   ,r.status    
		   ,r.image
		   ,r.start_date   
		   ,r.end_date
		   ,r.reg_date    
		   ,r.like_count    
		   ,r.views    
		FROM
		 tb_record_board AS r
		WHERE
			record_board_code = #{recordBoardCode};
	
	</select>
	
	<!-- 문화재 등록 SQL -->
	<insert id="addRecordBoard" parameterType="RecordBoard">
		INSERT INTO  tb_record_board
		(   record_board_code
		   ,member_id
		   ,record_board_name
		   ,content  
		   ,status    
		   ,image
		   ,start_date   
		   ,end_date
		   ,reg_date    
		   ,like_count    
		   ,views    
		) VALUES (
		   sf_new_record_board_code()
		   ,#{memberId}
		   ,#{recordBoardName}
		   ,#{content}
		   ,#{status}
		   ,#{image}
		   ,#{startDate}
		   ,#{endDate}
		   ,CURDATE()
		   ,#{likeCount}
		   ,#{views}
		);
	
	<!-- 문화재 수정 SQL -->
	</insert>
		<update id="modifyRecordBoard" parameterType="RecordBoard">
		UPDATE tb_record_board
		<trim prefix="SET" suffixOverrides=",">
			<if test="recordBoardName != null and recordBoardName != ''">
				record_board_name = #{recordBoardName},
			</if>
			<if test="content != null and content != ''">
				content = #{content},
			</if>
			<if test="status != null and status != ''">
				status = #{status},
			</if>
			<if test="image != null and image != ''">
				image = #{image},
			</if>
			<if test="startDate != null and startDate != ''">
				start_date = #{startDate},
			</if>
			<if test="endDate != null and endDate != ''">
				end_date = #{endDate},
			</if>		
			<if test="regDate != null and regDate != ''">
				reg_date = #{regDate},
			</if>				
			<if test="likeCount != null and likeCount != ''">
				like_count = #{likeCount},
			</if>				
			<if test="views != null and views != ''">
				views = #{views},
			</if>				
		</trim>
		WHERE
			record_board_code = #{recordBoardCode};
	</update>
</mapper>